* 目的
- 高速なコンパイル
- 簡潔な記述

* 概要
HappyABCはscmをswfに変換する際に、複数の中間形式を介します。
各中間形式の概要を以下に示します。

:abc-stage:scmをABC(ActionscriptByteCode)に変換します。
:abcx-stage:ABCをXML形式に変換します。
:swfx-stage:XML形式に変換したABCを、swf用のXMLファイルに埋め込みます。
:abc-stage:XML形式のswfを、バイナリ形式のswfに埋め込みます。
